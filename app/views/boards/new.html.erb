<div>
  <%= form_with model: [@organization, @board] do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :description %>
    <%= f.text_area :description %>
    <%= f.submit "Create Board" %>
  <% end %>
</div>
