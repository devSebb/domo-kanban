<div>
  <h1 class="text-black font-xl">My Organizations</h1>
  <ul class="">
    <% @organizations.each do |organization| %>
      <li class="mb-2">
        <%= link_to organization.name, organization_path(organization), class: "text-blue-600 hover:text-blue-800 underline" %>
      </li>
    <% end %>
  </ul>
</div>
