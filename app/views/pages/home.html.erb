<div class="p-6">
    <% if user_signed_in? %>
      <%= render "shared/user_dashboard" %>
  <div class="flex flex-col items-center mt-20">
    <% else %>
    <%= image_tag "dome_logo.svg", width: 120, height: 120 %>
    <h1 class="text-7xl font-semibold">Dome</h1>
      <p class="text-gray-500 mt-4 text-2xl">A project management tool, for your peace of mind.</p>
      <%= button_to "Get Started", new_user_registration_path, method: :get, class: "bg-tertiary text-white px-4 py-3 rounded-md mt-8 hover:bg-secondary transition-all duration-300" %>
    <% end %>
  </div>
</div>
